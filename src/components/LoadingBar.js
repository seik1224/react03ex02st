import React from "react";
import styles from "./LoadingBar.module.css"; // CSS 모듈 불러오기

const LoadingBar = () => {
  return <div className={styles.loadingBar}></div>; // CSS 모듈 클래스 사용
};

export default LoadingBar;
